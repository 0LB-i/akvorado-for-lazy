# Gerados pelo bootstrap/install (contêm credenciais reais)
.env
config/akvorado-orchestrator.yaml

# Dados persistentes e logs
data/
logs/
backups/

# Bancos MaxMind GeoLite2 - não redistribuir (licença MaxMind proíbe
# redistribuição pública). scripts/install.sh baixa isso com sua própria
# license key gratuita.
geoip/
